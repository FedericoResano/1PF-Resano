import { TamanoTituloDirective } from './tamano-titulo.directive';

describe('TamanoTituloDirective', () => {
  it('should create an instance', () => {
    const directive = new TamanoTituloDirective();
    expect(directive).toBeTruthy();
  });
});
